# Get it running with zapper in Cursor on macOS

## Install zapper:

```bash
npm install --global pm2 zapper-cli
```

## Install the extension
Install the cursor/vscode extension for Zapper:
`felixsebastian.zapper-vscode`

## Start

Open the zapper panel and click "Start All".
